# 🚀 更新：一键全局安装指南

## 重要更新

**默认安装行为已更改：现在一键安装默认将Agent安装到全局配置中！**

### 之前的版本：
- 默认：项目级安装（需要每次下载）
- 全局安装：需要显式指定 `--global`

### 现在的版本：
- ✅ **默认：全局安装**（共享所有项目）
- 项目级安装：需要显式指定 `--project`

## 📦 新的安装方式

### 方法1：一键全局安装（推荐）
```bash
# 最简单的方式 - 默认就是全局安装！
npm install -g https://github.com/jabing/my-opencode.git

# 然后在任何项目中：
oad deploy
# 或
my-opencode
# 或
opencode-setup
```

### 方法2：指定安装模式
```bash
# 全局安装（显式指定）
oad --global

# 项目级安装（需要显式指定）
oad --project
```

## 🎯 新的默认行为

### 默认（一键安装）：
```bash
# 运行以下命令：
oad

# 实际效果：
# 1. 检查OpenCode是否安装
# 2. 使用全局agent-constitution配置
# 3. 在当前项目创建.opencode/agents/文件
# 4. Agent文件共享给所有项目
```

### 全局安装的好处：
1. **一次安装，到处使用** - agent-constitution只下载一次
2. **节省时间** - 不需要每个项目都下载
3. **节省空间** - 避免重复文件
4. **一致性** - 所有项目使用相同版本的agent-constitution

## 🔧 实际演示

### 场景1：新用户第一次使用
```bash
# 1. 安装工具
npm install -g https://github.com/jabing/my-opencode.git

# 2. 在项目A中使用（自动全局安装）
cd ~/projects/project-a
oad
# ✅ agent-constitution安装到全局目录
# ✅ .opencode/agents/文件创建在project-a

# 3. 在项目B中使用（重用全局配置）
cd ~/projects/project-b
oad
# ✅ 直接使用全局的agent-constitution
# ✅ .opencode/agents/文件创建在project-b
```

### 场景2：需要项目级安装
```bash
# 特定项目需要独立配置
cd ~/projects/special-project
oad --project
# ✅ agent-constitution安装到项目vendor目录
# ✅ 与其他项目隔离
```

## 📁 安装位置

### 全局安装位置：
- **Windows**: `%APPDATA%\opencode\agent-constitution\`
- **Linux/macOS**: `~/.config/opencode/agent-constitution/`

### 项目文件位置：
- 每个项目的 `.opencode/agents/` 目录
- 包含指向全局配置的符号链接/引用

## 🔄 工作流程对比

### 之前的工作流程：
```
项目A: 下载 → 安装 → 使用
项目B: 下载 → 安装 → 使用
项目C: 下载 → 安装 → 使用
（每次都要下载）
```

### 现在的工作流程：
```
首次: 下载 → 安装到全局
项目A: 使用全局配置
项目B: 使用全局配置  
项目C: 使用全局配置
（只下载一次）
```

## ⚙️ 命令行选项

### 完整选项列表：
```bash
# 默认：全局安装
oad

# 显式全局安装
oad --global

# 项目级安装
oad --project

# 强制覆盖现有文件
oad --force

# 不禁用内置功能
oad --no-disable-builtins

# 指定agent-constitution源
oad --source /path/to/agent-constitution
```

### 帮助信息：
```bash
# 查看完整帮助
oad --help

# 输出：
# Options:
#   --force                 Overwrite existing files
#   --no-disable-builtins    Do not write .opencode/opencode.json to disable build/plan
#   --source <path>         Explicit path to agent-constitution source repo
#   --global               Install globally to user config directory (default)
#   --project              Install to project directory
```

## 🛠️ 迁移指南

### 从旧版本迁移：
```bash
# 1. 更新工具
npm install -g https://github.com/jabing/my-opencode.git

# 2. 清理旧的项目vendor目录（可选）
rm -rf vendor/agent-constitution

# 3. 在新项目中使用新默认行为
cd new-project
oad  # 自动使用全局安装
```

### 保留项目级安装：
```bash
# 如果特定项目需要独立配置
cd special-project
oad --project  # 显式指定项目级安装
```

## 📊 性能对比

### 时间对比：
- **全局安装**: ~2-3秒（首次下载后）
- **项目级安装**: ~10-15秒（每次都要下载）

### 空间对比：
- **全局安装**: ~5MB（共享）
- **项目级安装**: ~5MB × 项目数量

### 网络使用：
- **全局安装**: 首次使用下载一次
- **项目级安装**: 每个项目下载一次

## 🎉 总结

### 新的优势：
1. **更快的部署** - 重用全局配置
2. **更少的网络使用** - 只下载一次
3. **更简单的使用** - 一键安装就是最优配置
4. **更好的用户体验** - 默认就是最佳实践

### 向后兼容：
- `--project` 选项仍然可用
- 现有项目继续工作
- 帮助信息已更新

### 推荐用法：
```bash
# 对于大多数用户：
npm install -g https://github.com/jabing/my-opencode.git
oad  # 在任何项目中

# 对于需要隔离的项目：
oad --project
```

**现在的一键安装更加智能、快速和高效！** 🚀